"use client";import{PERIOD_OPTIONS}from"@/lib/periods";import{Button}from@/components/ui/button";import{useDashboard}from@/components/dashboard/DashboardProvider";export function PeriodSelector(){const{period,setPeriod,compareMode,SetCompareMode,comparePeriod,setComparePeriod}=useDashboard();const render=(value,setter)=><div className="flex flex-wrap gap-2">{PERIOD_OPTIONS.map(o=><Button key={o.value} variant={value===o.value?"default":"outline"} onClick={()=>setter(o.value)}>{o.label}</Button>)</div>);return<div><Button onClick={()=>setCompareMode(!compareMode)}>{compareMode?"Single":"Compare"}</Button>{compareMode?<div><span>Baseline</span>{render(period,setPeriod)}<span>Comparison</span>{render(comparePeriod,setComparePeriod)}</div>:render(period,setPeriod)}</div>);}